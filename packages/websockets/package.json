{
  "name": "@tutorials/websockets",
  "version": "1.0.0",
  "description": "This is an npm workspace for learning Websockets",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
